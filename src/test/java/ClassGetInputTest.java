/*
import org.junit.Test;

import java.io.InputStream;

*/
/**
 * @Author liudi
 * @Date Create in 15:43 2018/10/29
 *//*

public class ClassGetInputTest {

    @Test
    public void test(){
        // 通过传入的文件名去加载对应文件
        // 当传入的参数是没有”/”的时候，获取的是当前类所在包下的对应文件。而当参数带有”/”，则是从ClassPath根目录下获取文件
        InputStream resourceAsStream = ClassGetInputTest.class.getResourceAsStream("/load.properties");
        System.out.println(resourceAsStream.toString());

    }
}
*/
